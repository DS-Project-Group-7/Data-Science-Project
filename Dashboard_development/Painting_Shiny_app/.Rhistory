#    excellent = 0
#    )
#  ) %>%
group_by(Country) %>%
summarise(
#  Auxiliary.support.Condition = count(Auxiliary.support.Condition)
country = count(Country)
#  fair = sum(fair, na.rm = TRUE),
#  good = sum(good, na.rm = TRUE),
#  excellent = sum(excellent, na.rm = TRUE)
)
bar_data <- clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
group_by(Country) %>%
summarise(
#  Auxiliary.support.Condition = count(Auxiliary.support.Condition)
country = count(Country)
#  fair = sum(fair, na.rm = TRUE),
#  good = sum(good, na.rm = TRUE),
#  excellent = sum(excellent, na.rm = TRUE)
)
bar_data
bar_data <- clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
group_by(Country,Auxiliary.support.Condition) %>%
summarise(
Auxiliary.support.Condition = count(Auxiliary.support.Condition)
#country = count(Country)
#  fair = sum(fair, na.rm = TRUE),
#  good = sum(good, na.rm = TRUE),
#  excellent = sum(excellent, na.rm = TRUE)
)
bar_data
bar_data <- clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
group_by(Country)
bar_data
bar_data <- clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
group_by(Country,Auxiliary.support.Condition)
bar_data
bar_data <- clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
group_by(Country) %>%
dplyr::summarise(
Auxiliary.support.Condition = count(Auxiliary.support.Condition)
#  fair = sum(fair, na.rm = TRUE),
#  good = sum(good, na.rm = TRUE),
#  excellent = sum(excellent, na.rm = TRUE)
)
bar_data
?summarise
detach("package:plyr", unload=TRUE)
detach("package:reshape2", unload=TRUE)
detach("package:plyr", unload=TRUE)
?summarise
detach("package:plyr", unload=TRUE)
bar_data <- clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
group_by(Country) %>%
dplyr::summarise(
Auxiliary.support.Condition = count(Auxiliary.support.Condition)
#  fair = sum(fair, na.rm = TRUE),
#  good = sum(good, na.rm = TRUE),
#  excellent = sum(excellent, na.rm = TRUE)
)
bar_data <- clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
group_by(Country) %>%
dplyr::summarise(
Auxiliary.support.Condition = count(Auxiliary.support.Condition)
#  fair = sum(fair, na.rm = TRUE),
#  good = sum(good, na.rm = TRUE),
#  excellent = sum(excellent, na.rm = TRUE)
)
bar_data <- clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
group_by(Country) %>%
dplyr::summarise(
Auxiliary.support.Condition = count(Auxiliary.support.Condition)
#  fair = sum(fair, na.rm = TRUE),
#  good = sum(good, na.rm = TRUE),
#  excellent = sum(excellent, na.rm = TRUE)
)
library(shiny)
library(shinydashboard)
library(ggplot2)
library(readxl)
library(dplyr)
library(tidyverse)
clean_data <-
read.csv(
"data/cleanData.csv"
)
bar_data <- clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
group_by(Country) %>%
dplyr::summarise(
Auxiliary.support.Condition = count(Auxiliary.support.Condition)
#  fair = sum(fair, na.rm = TRUE),
#  good = sum(good, na.rm = TRUE),
#  excellent = sum(excellent, na.rm = TRUE)
)
?count
str(clean_data$Auxiliary.support.Condition)
clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
group_by(Country) %>%
dplyr::summarise(
Auxiliary.support.Condition =count(Auxiliary.support.Condition,name = aux_frq)
#  fair = sum(fair, na.rm = TRUE),
#  good = sum(good, na.rm = TRUE),
#  excellent = sum(excellent, na.rm = TRUE)
)
?summarise
?group_by
clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
dplyr::group_by(Country)
clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
dplyr::group_by(Country) %>%
dplyr::summarise(
Auxiliary.support.Condition =count(Auxiliary.support.Condition,name = aux_frq)
#  fair = sum(fair, na.rm = TRUE),
#  good = sum(good, na.rm = TRUE),
#  excellent = sum(excellent, na.rm = TRUE)
)
clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
dplyr::group_by(Country) %>%
dplyr::summarise(
Auxiliary.support.Condition =sum(Auxiliary.support.Condition)
#  fair = sum(fair, na.rm = TRUE),
#  good = sum(good, na.rm = TRUE),
#  excellent = sum(excellent, na.rm = TRUE)
)
?summarise
clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
dplyr::group_by(Country) %>%
dplyr::summarise(
Auxiliary.support.Condition =n_distinct(Auxiliary.support.Condition)
#  fair = sum(fair, na.rm = TRUE),
#  good = sum(good, na.rm = TRUE),
#  excellent = sum(excellent, na.rm = TRUE)
)
clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
dplyr::group_by(Country) %>%
dplyr::summarise(
Auxiliary.support.Condition =n(Auxiliary.support.Condition)
#  fair = sum(fair, na.rm = TRUE),
#  good = sum(good, na.rm = TRUE),
#  excellent = sum(excellent, na.rm = TRUE)
)
clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
group_by(Country) %>%
summarise(
Auxiliary.support.Condition = n()
)
clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
group_by(Country) %>%
summarise(
Auxiliary.support.Condition = list(cur_group())
)
clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
group_by(Country) %>%
summarise(
Auxiliary.support.Condition = cur_group()
)
clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
group_by(Country,Auxiliary.support.Condition) %>%
summarise(
Auxiliary.support.Condition = cur_group()
)
clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
group_by(Country) %>%
summarise(
Auxiliary.support.Condition = cur_group_rows()
)
clean_data %>%
select(
Country,
Auxiliary.support.Condition
#Media.Condition,
#Painting.support.condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
group_by(Country) %>%
summarise(
Auxiliary.support.Condition = cur_data(Auxiliary.support.Condition)
)
clean_data %>%
select(
Country,
Auxiliary.support.Condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
group_by(Country) %>%
summarise(
Auxiliary.support.Condition = cur_group()
)
bar_data <- clean_data %>%
select(
Country,
Auxiliary.support.Condition
)
bar_data
bar_data%>%group_by(Country)%>%count(Auxiliary.support.Condition)
bar_data <- clean_data %>%
select(
Country,
Auxiliary.support.Condition
) %>%
#replace_na(
#  list(
#    poor = 0,
#    fair = 0,
#    good = 0,
#    excellent = 0
#    )
#  ) %>%
group_by(Country) %>%
count(Auxiliary.support.Condition)
bar_data_2 <-bar_data %>% gather(status, count, -country)
bar_data_2 <-bar_data %>% gather(status, n, -Country)
bar_data_2
bar_data %>% gather(status, count, -Country)
bar_data %>% gather(status, n, -Country)
bar_data
bar_data %>% gather(status, Auxiliary.support.Condition, -Country)
bar_data
